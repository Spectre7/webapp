CREATE TABLE TAG (ID BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SLIDE (ID BIGINT NOT NULL, BODY VARCHAR(255), TITLE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SLIDE_TAG (Slide_ID BIGINT NOT NULL, tag_ID BIGINT NOT NULL, PRIMARY KEY (Slide_ID, tag_ID))
ALTER TABLE SLIDE_TAG ADD CONSTRAINT FK_SLIDE_TAG_Slide_ID FOREIGN KEY (Slide_ID) REFERENCES SLIDE (ID)
ALTER TABLE SLIDE_TAG ADD CONSTRAINT FK_SLIDE_TAG_tag_ID FOREIGN KEY (tag_ID) REFERENCES TAG (ID)
